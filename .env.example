# Environment variables — copy to `.env` (never commit `.env`)

NODE_ENV=development
PORT=4000
# Local: use the same URL for both. Neon/Vercel: pooled DATABASE_URL + direct DIRECT_URL (see Neon dashboard).
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/sca_dev
DIRECT_URL=postgresql://postgres:postgres@localhost:5432/sca_dev
JWT_ACCESS_SECRET=change-me-access-secret-min-32-chars
JWT_REFRESH_SECRET=change-me-refresh-secret-min-32-chars
JWT_ACCESS_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d
CORS_ORIGIN=http://localhost:3000
