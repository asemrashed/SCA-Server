# SCA Server

Standalone API. **Guide docs are at the workspace root** (`../` — see parent `README.md`).

```bash
cp .env.example .env
npm install
npx prisma generate
npm run dev    # http://localhost:4000/api
```

Read before coding: `../AGENT-LANES.md`, `../API-CONTRACT.md`, `../SCHEMA.md`, `../cursorrules`.
